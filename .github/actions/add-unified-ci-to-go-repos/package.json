{
  "name": "add-unified-ci-to-go-repos",
  "description": "Add Unified CI to Go repositories",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "prepare": "ncc build index.js -o dist --source-map --license license.txt",
    "test": "jest --passWithNoTests",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.2.5",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1",
    "eslint": "^8.0.0",
    "jest": "^27.2.5"
  }
}
